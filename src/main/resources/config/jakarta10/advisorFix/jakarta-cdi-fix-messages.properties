empty-beans-xml=If necessary, you can set the cdiemptyBeansXmlCDI3Compatibility attribute to enable compatibility with previous versions. \n \
  Setting this attribute to true in CDI 4.0 causes an empty beans.xml file to be treated as an explicit beans archive.
jakarta-cdi-remove-annotation-new=CDI applications must inject Dependent scoped beans instead.
jakarta-cdi-remove-is-nullable=It is safe to just remove this method from any custom implementation of Bean interface as it was NOT used.
jakarta-cdi-method-change-fire-event=BeanManager.getEvent should be used instead.
jakarta-cdi-method-change-create-injection-target=BeanManager.getInjectionTargetFactory(AnnotatedType) should be used instead.
jakarta-cdi-method-change-add-annotated-type=BeforeBeanDiscovery#addAnnotatedType(AnnotatedType, String) should be used instead.
jakarta-cdi-file-empty-beans-xml=If necessary, you can set the cdiemptyBeansXmlCDI3Compatibility attribute to enable compatibility with previous versions. \n \
  Setting this attribute to true in CDI 4.0 causes an empty beans.xml file to be treated as an explicit beans archive.
jakarta-cdi-file-not-found-beans-xml=Annotate all beans used by the application